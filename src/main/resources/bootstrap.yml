spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/prabasingaravel/cloud-config-repo
        bootstrap: true
management:  endpoints:    web:      exposure:        include: "*"

server:
  port: 8888